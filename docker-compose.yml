services:
  postgres:
    image: postgres:17-alpine
    container_name: larsa-play-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: larsa
      POSTGRES_PASSWORD: larsa
      POSTGRES_DB: larsa_play
    ports:
      - "5432:5432"
    volumes:
      - larsa_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U larsa -d larsa_play"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  larsa_postgres_data:
